{
    "appName": "suggester-laesekompas-webservice",
    "jdbc": {
        "jdbc/holdings-items": {
            "url": "${HOLDINGS_ITEMS_POSTGRES_URL}",
            "maxSize": "${MAX_POOL_SIZE}"
        }
    }
}